---
interface Props {
  id: string;
  title: string;
}

const { id, title } = Astro.props as Props;
const checkboxId = `legal-modal-${id}`;
const headingId = `${checkboxId}-title`;
---

<div class="relative">
  <input id={checkboxId} type="checkbox" class="peer hidden" />

  <label
    for={checkboxId}
    class="cursor-pointer text-slate-300 hover:text-[var(--ds-primary)]"
  >
    {title}
  </label>

  <div
    class="pointer-events-none fixed inset-0 z-50 flex items-center justify-center bg-[var(--ds-overlay)] opacity-0 transition peer-checked:pointer-events-auto peer-checked:opacity-100"
  >
    <label for={checkboxId} class="absolute inset-0" aria-hidden="true"></label>

    <div
      role="dialog"
      aria-modal="true"
      aria-labelledby={headingId}
      class="relative mx-4 max-h-[80vh] w-full max-w-lg overflow-hidden rounded-2xl border border-[var(--ds-border-subtle)] bg-[var(--ds-surface)] p-5 text-sm text-slate-800 shadow-[0_24px_60px_rgba(15,23,42,0.22)]"
    >
      <header class="mb-3 flex items-start justify-between gap-4">
        <h2 id={headingId} class="text-sm font-semibold text-slate-900">
          {title}
        </h2>
        <label
          for={checkboxId}
          class="flex h-8 w-8 cursor-pointer items-center justify-center rounded-full border border-[var(--ds-border-subtle)] bg-white text-xs text-slate-600 hover:border-[var(--ds-border-soft)] hover:text-slate-900"
          aria-label="Jogi ablak bezárása"
        >
          ✕
        </label>
      </header>

      <div class="max-h-[52vh] space-y-2 overflow-y-auto pr-1 text-xs leading-relaxed text-slate-700">
        <slot />
      </div>

      <div class="mt-4 flex justify-end text-[0.7rem] text-slate-500">
        <label
          for={checkboxId}
          class="cursor-pointer hover:text-slate-900"
        >
          Bezárás
        </label>
      </div>
    </div>
  </div>
</div>

